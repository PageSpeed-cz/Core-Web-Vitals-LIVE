{
  "name": "core-web-vitals-live",
  "description": "See your website's performance. Live. Core Web Vitals (LCP, INP, CLS) overlay and visualizations.",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "dev:firefox": "wxt -b firefox",
    "privacy-page": "node scripts/generate-privacy-html.mjs",
    "prebuild": "npm run privacy-page",
    "build": "wxt build",
    "build:firefox": "npm run privacy-page && wxt build -b firefox",
    "prezip": "npm run privacy-page",
    "zip": "wxt zip",
    "zip:firefox": "npm run privacy-page && wxt zip -b firefox",
    "compile": "tsc --noEmit",
    "postinstall": "wxt prepare"
  },
  "devDependencies": {
    "marked": "^15.0.12",
    "typescript": "^5.9.3",
    "wxt": "^0.20.18"
  },
  "dependencies": {
    "web-vitals": "^5.1.0"
  }
}
